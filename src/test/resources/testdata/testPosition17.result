 let j = 1;
 console.log("-> <selection>rows</selection>", <selection>rows</selection>);
    if (rows === "something") {
      let foo = { prop: cols };
      console.log("-> cols", cols);
      foo.other = "other";
    }